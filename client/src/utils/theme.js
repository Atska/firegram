import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#fb8c00",
      contrastText: "#000"
    },
    secondary: {
      main: "#f48fb1"
    }
  }
});

export default theme;
