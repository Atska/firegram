import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#fb8c00",
      contrastText: "#000"
    },
    secondary: {
      main: "#d50000"
    }
  }
});

export default theme;
